# Production / `vite preview`: full URL of your FastAPI API (no trailing slash).
# Local `npm run dev`: this value is ignored — requests use /api/* and Vite proxies
# to http://127.0.0.1:8000 (see vite.config.js). Ensure the backend is running.
VITE_API_URL=https://your-api.onrender.com
